name: Update Task List
on:
  issues:
    types: [opened, edited, closed, reopened, labeled, unlabeled]
  workflow_dispatch:

jobs:
  update-status-file:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.ADMIN_BOT_TOKEN }}

      - name: Generar Lista de Tareas Pendientes
        uses: actions/github-script@v6
        with:
          script: |
            const fs = require("fs");

            // Leer grupos desde JSON
            const groupsFile = `${process.env.GITHUB_WORKSPACE}/grupos.json`;
            if (!fs.existsSync(groupsFile)) {
              throw new Error(`No se encuentra el archivo grupos.json en la raíz del repo: ${groupsFile}`);
            }
            const groups = JSON.parse(fs.readFileSync(groupsFile, "utf8"));

            // Obtener issues abiertos
            const issues = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: "open"
            });

            let content = "# 📋 Tareas Pendientes\n\n";

            function getPriority(issue) {
              const label = issue.labels.find(l => l.name.startsWith("Prioridad:"));
              return label ? label.name.replace("Prioridad: ", "") : "Media";
            }

            function priorityWeight(priority) {
              if (priority === "Alta") return 1;
              if (priority === "Media") return 2;
              if (priority === "Baja") return 3;
              return 4;
            }

            for (const groupName in groups) {
              const members = groups[groupName];
              const groupIssues = issues.data.filter(issue =>
                issue.labels.some(l => l.name === groupName)
              );

              if (groupIssues.length === 0) continue;

              content += `## 👥 ${groupName}\n\n`;

              groupIssues.sort((a, b) =>
                priorityWeight(getPriority(a)) - priorityWeight(getPriority(b))
              );

              for (const issue of groupIssues) {
                const labels = issue.labels.map(l => l.name);
                const esGrupal = labels.includes("Tipo: Grupal");

                if (esGrupal) {
                  // Tarea grupal
                  const completadoGrupo = labels.includes(`Completado: ${groupName}`);
                  if (!completadoGrupo) {
                    content += `### 📌 Tareas Grupales\n`;
                    content += `- (${getPriority(issue)}) ${issue.title} (#${issue.number})\n\n`;
                  }
                  continue;
                }

                // Tarea individual
                const userLabel = labels.find(l => l.startsWith("Usuario: "));
                if (userLabel) {
                  const username = userLabel.replace("Usuario: ", "");
                  const completado = labels.includes(`Completado: ${username}`);
                  if (!completado) {
                    content += `### 👤 ${username}\n`;
                    content += `- (${getPriority(issue)}) ${issue.title} (#${issue.number})\n\n`;
                  }
                } else {
                  // Si no hay label de usuario, asigna a todos los miembros del grupo
                  for (const member of members) {
                    const completado = labels.includes(`Completado: ${member}`);
                    if (!completado) {
                      content += `### 👤 ${member}\n`;
                      content += `- (${getPriority(issue)}) ${issue.title} (#${issue.number})\n\n`;
                    }
                  }
                }
              }

              content += "\n";
            }

            fs.writeFileSync(`${process.env.GITHUB_WORKSPACE}/ESTADO.md`, content);

      - name: Commit and Push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git add ESTADO.md
          git commit -m "Actualizar lista de tareas" || exit 0
          git push origin main
